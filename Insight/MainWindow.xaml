<Window x:Class="Insight.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="800" Width="1000">
    
    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Grid Grid.Column="0">
            
            <StackPanel>
                <Button
                    x:Name="btnTimelineZoomIn"
                    Height="50"
                    Content="Zoom In"
                    Click="btnTimelineZoomIn_Click"
                    Margin="2"
                    />
                
                <Button
                    x:Name="btnTimelineZoomOut"
                    Height="50"
                    Content="Zoom Out"
                    Click="btnTimelineZoomOut_Click"
                    Margin="2"
                    />
                
                <Button
                    x:Name="btnTimelineZoomReset"
                    Height="50"
                    Content="Reset Zoom"
                    Click="btnTimelineZoomReset_Click"
                    Margin="2"
                    />

                <Button
                    x:Name="btnTimelineReload"
                    Height="50"
                    Content="Reload Data"
                    Click="btnTimelineReload_Click"
                    Margin="2"
                    />
            </StackPanel>

        </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="500"/>
                <RowDefinition Height="100" />
            </Grid.RowDefinitions>
            <UserControl 
            xmlns:timeline="clr-namespace:TimelineLibrary;assembly=TimelineLibrary"
            xmlns:timelineEx="clr-namespace:TimelineEx;assembly=TimelineExLib"
            Height="500"  Grid.Row="0">

                <timelineEx:TimelineTrayEx
                x:Name="timeline"
                Height="500"
                Grid.Row="0"
                CalendarType="gregorian"
                HorizontalAlignment="Stretch"
                MinDateTime="01/01/2006"
                MaxDateTime="01/01/2007"
                CurrentDateTime="06/01/2006"
                ImmediateDisplay="False"
                SelectionChanged="timeline_SelectionChanged"
                EditMode="True"
                >

                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="60"/>
                        <RowDefinition Height="60"/>
                    </Grid.RowDefinitions>

                    <timelineEx:TimelineTrayEx.Urls>
                        <timeline:UriInfo Url="http://localhost:8080/example.xml" />
                    </timelineEx:TimelineTrayEx.Urls>

                    <timelineEx:TimelineBandEx
                        x:Name="hourTimeBand"
                        Grid.Row="0"
                        IsMainBand="True"
                        ItemSourceType="hours"
                        HorizontalAlignment="Stretch"
                        TimelineWindowSize="20"
                        MaxEventHeight="250"
                   />

                    <timelineEx:TimelineBandEx
                    Grid.Row="1"
                    Height="60"
                    HorizontalAlignment="Stretch"
                    ItemSourceType="days"
                    TimelineWindowSize="16"
                    MaxEventHeight="4"
                    />

                    <timelineEx:TimelineBandEx
                    Grid.Row="2"
                    Height="60"
                    HorizontalAlignment="Stretch"
                    ItemSourceType="months"
                    TimelineWindowSize="12"
                    MaxEventHeight="4"
                    />



                </timelineEx:TimelineTrayEx>



            </UserControl>
        </Grid>

    </Grid>
    
    
</Window>
